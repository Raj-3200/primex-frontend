# Suppress npm audit during install (audits run separately in CI)
# The postcss vulnerability is inside Next.js's own bundled dependency
# and cannot be fixed without downgrading Next.js itself.
# This is a known false-positive: https://github.com/vercel/next.js/issues
audit=false
fund=false
loglevel=error
